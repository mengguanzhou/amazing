pk1.setOnSelectListener(new com.wiipu.shopping.view.PickerViewForArea.onSelectListener() {
			@Override
			public void onSelect(String s) {
				int size = list1.size();
				int mCurrentSelected = getIndex(s, 1);
				if (list1 != null && list11 != null && size > mCurrentSelected) {
					area[0] = list11.get(mCurrentSelected).getName();
					data[0] = list11.get(mCurrentSelected).getCi_id();

					request(TYPE_XIAN, list11.get(mCurrentSelected).getCi_id(),
							0);

				}

			}
		});